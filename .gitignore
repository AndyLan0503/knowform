# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# knowform lockfile is committed by consumers; ignored only in scratch
# (this repo dogfoods and commits its own knowform.lock intentionally)
